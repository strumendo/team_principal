# ============================================================
# TeamPrincipal — Environment Variables
# Copy this file to .env and fill in the values
# Copie este arquivo para .env e preencha os valores
# ============================================================

# ---- Application / Aplicacao ----
APP_NAME=TeamPrincipal
APP_ENV=development
DEBUG=true

# ---- Backend / FastAPI ----
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
BACKEND_CORS_ORIGINS=["http://localhost:3000"]

# ---- Database / Banco de Dados (PostgreSQL) ----
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=team_principal
POSTGRES_PASSWORD=changeme
POSTGRES_DB=team_principal

# Async connection URL (used by the application)
# URL de conexao assincrona (usada pela aplicacao)
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Sync connection URL (used by Alembic migrations)
# URL de conexao sincrona (usada pelas migracoes Alembic)
DATABASE_URL_SYNC=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ---- Auth / Autenticacao (JWT) ----
SECRET_KEY=change-this-to-a-random-secret-key
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ---- Frontend / Next.js ----
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-this-to-a-random-nextauth-secret

# ---- AWS (future / futuro) ----
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=
